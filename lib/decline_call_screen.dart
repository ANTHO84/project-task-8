import 'package:flutter/material.dart';

class DeclineCallScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'ĐÃ TỪ CHỐI CUỘC GỌI',
          style: TextStyle(fontSize: 24, color: Colors.red),
        ),
      ),
    );
  }
}
